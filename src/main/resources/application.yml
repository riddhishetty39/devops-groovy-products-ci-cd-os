server:
  port: 8080
  
spring:
  datasource:
    url: jdbc:h2:mem:products
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
  
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
